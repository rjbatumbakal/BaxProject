import Text.Printf (printf)

main :: IO ()
main = do
    
    line <- getLine
    let parts = words line

    let scores = map read parts :: [Double]

    if length scores == 3 && all (\n -> n >= 1 && n <= 20) scores
        then printf "%.2f\n" (sum scores / 3)
        else return ()
